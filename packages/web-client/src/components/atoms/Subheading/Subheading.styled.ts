import styled from 'styled-components';
import colors from '../../../styles/colors';

export const StyledSubheading = styled.p`
  font-size: 0.875em;
  font-weight: 500;
  color: ${colors.slate.c500};
`;
